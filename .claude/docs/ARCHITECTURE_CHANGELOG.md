# ARCHITECTURE_CHANGELOG.md — RTSH-OTT

Full dated history of architecture decisions, moved out of `rules/ARCHITECTURE.md`
on 2026-07-03 to keep that file lean — it's auto-loaded every session; this file
is read on demand (git-log-adjacent, not standing context).

**Append new entries here** (newest at the top, matching the order below) —
never grow this log back inline in `ARCHITECTURE.md`. Each entry:
`- **YYYY-MM-DD** — **Title.** Body.` See `CLAUDE.md → Doc sync` for when to log one.

---

- **2026-07-03** — **Expo SDK 56 → 57 upgrade (RN 0.85.3 → 0.86.0, React 19.2.3).** Ran the `expoUpgrade` chain (`expo install expo@latest` → `--fix`). SDK 57 is a low-risk major (RN 0.86 has no breaking changes from 0.85 per Expo; no removed/deprecated APIs; Hermes V1 Android-memory issue N/A — not opted in). Two npm ERESOLVE blockers the script hit were fixed by hand: **(1)** stale `package-lock.json` still pinned RN 0.85.3 (the script deletes `node_modules` but not the lockfile) → regenerated the lock; **(2)** `react-test-renderer`'s `^19.2.3` range floated to 19.2.7, whose peer tightened to `react@^19.2.7` while `react` stays pinned at 19.2.3 → pinned `react-test-renderer` to exact `19.2.3` (React's must-match-exactly rule). Also aligned two devDeps `--fix` never reached before crashing: `jest-expo` `~56.0.5`→`~57.0.1` (peers `@react-native/jest-preset@^0.86.0` ✅), `eslint-config-expo` `~56.0.4`→`~57.0.0`. Verified: expo-doctor 20/20, tsc clean, lint clean, 74/74 tests pass. **Native rebuild required** (CNG: local `ios/`/`android/` are gitignored SDK 56 artifacts → `expo prebuild --clean` before a local build; EAS prebuilds fresh from git). Context: triggered by an app crash after an `eas update` shipped native changes onto an SDK 56 binary — OTA carries JS/assets only. **Reminder:** install the new binary; don't OTA into the old one. (Open follow-up, not yet done: switch `runtimeVersion.policy` `'appVersion'`→`'fingerprint'` so EAS refuses to serve an OTA to a native-incompatible build instead of crashing it.)
- **2026-07-03** — **Backend sync (Henri): deleted dead playback re-sign scaffolding + added `deviceClass` to the WS handshake.** Backend confirmed the viewing-session contract is fully in sync (`/app/watch` + `/app/watch.end` are now the authoritative counting trigger; re-fires on reconnect ✅; no client SEND to broker destinations ✅; no `/app/watch` keepalive ✅; no DRM calls ✅). Two follow-ups from their §4/§3: **(1) Removed the media-plane session (§4.1).** The backend confirmed the decision response is exactly `{ decision, channelId, programId, noticeMessage, streams }` — there is no `sessionId`/`expiresAt` and no `POST /channels/playback/refresh`. The re-sign scaffolding (disabled 2026-06-28, "kept for easy re-enable") is now fully deleted: `refreshPlayback()` service + `CHANNELS_ROUTES.PLAYBACK_REFRESH`, `sessionId`/`expiresAt` off the `PlaybackDecision` domain type + `playbackDecisionDtoSchema` + `toPlaybackDecision`, both mock occurrences, the mock refresh POST handler, and the preserved reference-implementation block in `docs/API.md`. Zero-risk (the service had no callers; grep-confirmed). Stream-URL longevity stays a signed/expiring/IP-bound-URL concern on the origin, not a client re-sign. **(2) Added `?deviceClass=MOBILE|TV|STB` to the WS handshake URL (§3, optional).** `WS_URL` (`realtime/events.ts`) now appends `getDeviceClass()` at module load (device identity is static at runtime) so the backend can attribute the viewer in the live by-platform counter — coarse class only, no `deviceKey`/model/OS (those still ride `PUT /users/me/device`). `docs/API.md` (Channels) + `docs/REALTIME_SOCKET.md` (§1 out-of-scope note + §2 handshake) synced. tsc + lint clean. **JS-only — no native rebuild.** Device-registration mutation deliberately **kept** — it carries durable device inventory + `appVersion` (the only remaining channel for it since device headers were dropped), distinct from the socket's ephemeral counter attribution.
- **2026-07-03** — **Ad overlap fix: route-scoped app-open ad + app-wide single-ad-slot guard.** Bug: the app-open `AdOverlay` is mounted above the `Stack` in `(app)/_layout.tsx` (outside navigation, so it doesn't unmount on route change); navigating into `channel/[id]` before its `AD_REVEAL_DELAY_MS` reveal delay elapsed could leave it showing at the same time as that screen's own `CHANNEL_CHANGE` preroll — two native `Modal`s stacked. Fixed with two independent layers (both requested by the user as "industry standard" rather than relying on timing alone): (1) **Route scope** — the app-open ad (including its `useDelayedReveal` reveal timer) is now gated on `usePathname()` being one of the 4 tabs paths (`TAB_PATHS`, `(app)/_layout.tsx`); navigating away mid-countdown cancels the reveal outright rather than letting it fire later on the wrong screen. Channel-change/mid-roll need no equivalent change — they already live inside `channel/[id].tsx`'s own component tree, so they structurally can't render anywhere else. (2) **One ad on screen at a time, app-wide** — new `AdsSlice` (`activeAdId: number | null` + `updateAdsSlice`, same single-owner shape as `ModalSlice.currentModal`) + new `useAdSlot(wantsToShow, adId)` hook (`hooks/useAdSlot.ts`): the first ad whose full readiness condition goes true claims the slot in a mount effect and releases it on unmount/dismiss; a second ad wanting the slot while it's held is simply not granted (its render condition returns `false`, no flash-then-unmount). All three placements (`APP_OPEN`, `CHANNEL_CHANGE`, `MID_ROLL`) route through it. Route scope handles the reported steady-state bug; the slot guard additionally closes the narrower screen-transition-animation window (a `fullScreenModal` push animates ~300ms, during which the outgoing and incoming screens can both be mounted) and protects any future 4th ad placement. tsc + lint + 74 jest tests clean. **JS-only — no native rebuild.** No backend/API change.
- **2026-07-03** — **Component tests (AdOverlay + ParentalPinModal) + dead-EPG deletion (user-approved).** (1) **Dead code deleted:** `getEpgByDate`/`getProgramById` (services/epg), `useEpgQuery` (queries), `EPG_ROUTES` (`/epg`, `/epg/program/{id}`) and both mock handlers — zero consumers; per-channel EPG rides `CHANNELS_ROUTES.EPG`, the Guide rides `GUIDE_ROUTES`, recorded playback rides `CATCHUP_PLAYBACK`. `useChannelEpgQuery` (same file) untouched. (2) **RNTL component tests** (closes audit plan 1.2): `AdOverlay` — completion + impression fire exactly once across all three dismissal paths (duration timer / skip / video natural end), watched-seconds clamped, the NaN/0-duration failsafe auto-dismisses (the historical app-lock bug is now pinned by a test), skip hidden for non-skippable + inert until `skipAfterSeconds`; `ParentalPinModal` — verify success/failure against the REAL store slice, 5-strike lockout (message shown, keys inert, correct PIN refused), set-mode confirm-mismatch rejected, stored value always a SHA-256 hash. (3) **Component-test infra:** jest `resolver: react-native-worklets/jest/resolver.js` (reanimated 4 under jest) + `jest.setup.ts` in `setupFilesAfterEnv` (reanimated `setUpTests()`, safe-area official mock, in-memory MMKV stand-in matching `store/storage.ts`'s API). Suite now **74 tests / 10 suites**, ~1s. tsc + lint clean. **JS/dev-only — no native rebuild.**
- **2026-07-03** — **Audit Phase 2: Zod boundaries, country picker in-house, a11y pass, persist whitelist, analytics cleanup.** (1) **Zod at the non-auth boundaries (closes 5.X.2/11.Y.5, loose-by-design):** `channelDtoSchema`/`playbackDecisionDtoSchema` (services/channels), `guideProgramDtoSchema`/`guideChannelDtoSchema`/`adDtoSchema` (types/domain). Load-bearing fields fail loud; decorative fields `.catch()` defaults; unknown keys pass through (contract still settling). Ads validate per element (safeParse + drop). **Bug found + fixed by this work:** `mapGuideProgram` dropped `decision`/`noticeMessage` from real-API EPG rows — the per-programme geo gate could never fire against the real backend; both now mapped (+ added to `GuideProgramDto`). (2) **`react-native-country-picker-modal` removed** (unmaintained; had forced the web-platform workaround): `CountryPickerInput` now renders an in-house searchable sheet (SearchBar + FlatList, fixed-row `getItemLayout`) over new static `constants/countries.ts` (250 ISO-3166 entries generated from the old lib's own dataset → selectable names byte-identical, backend payload unchanged; flag emoji derived from the ISO code — no assets). (3) **A11y pass:** `accessibilityRole`/`Label`/`State` on every previously-bare interactive leaf (StationRow, ProgramRow, GuideRow, HeroCarousel, RadioMiniPlayer, ParentalPinPad, ModalWrapper backdrop, OtpVerify resend, AuthFooterLink, SectionHeader, CountryPicker rows); new i18n `common.play/pause/delete` (en+sq); convention written into STYLE_GUIDE → Key points. Device VoiceOver/TalkBack smoke test pending. (4) **`user` persist whitelist (5.X.17):** `persistUser()` in the partialize — explicit field list so a future sensitive `User` field can't silently persist to plaintext MMKV. (5) **Analytics stays OFF** (backend ingestion pending): dead commented imports/mounts removed; pointer comments remain at the three mount sites. Also flagged: `useEpgQuery`/`getEpgByDate` + `getProgramById` have zero consumers (deletion candidate, awaiting user decision). tsc + lint + 64 jest tests clean. **JS-only — no native rebuild** (dep removal lands with the next regular build).
- **2026-07-03** — **Audit Phase 1: test foundation + CI + root ErrorBoundary (Sentry deferred by user).** (1) **Testing** — `jest-expo` + `@testing-library/react-native` + `react-test-renderer@19.2.3` (pinned to the SDK's react); jest config in `package.json` (preset, `@/` moduleNameMapper, `clearMocks`), `tsconfig` `types: [jest, node]`, scripts `test`/`test:watch`. **64 tests / 8 suites** over the pure core: `realtime/midroll` (session-window, fire-now, lapse, boundary), `utils/epg`, `utils/datetime` (local day-key, h23 pin), `lib/tokenVault` (remember-me matrix incl. cold-boot hydration + rotation), `utils/resolveStreamSource` (extensionless→hls fix), `utils/pin` (SHA-256 via Node-crypto mock), `utils/image`, and `api/mutations/authRefresh` behavior (single-flight, transient-no-logout, 401→logout+cache-wipe+notify). (2) **CI** — `.github/workflows/ci.yml` (PR+main: tsc, lint `--max-warnings 0`, `jest --ci`; hermetic mock mode) + `deps-health.yml` (weekly expo-doctor + high+ audit). (3) **Root `ErrorBoundary`** exported from `app/_layout.tsx` — dependency-free branded retry (closes the long-standing boot known-gap). (4) **Upgrade tooling** — `npm run expoUpgrade` (official chain: `expo install expo@latest` → `--fix` → clean reinstall → doctor) + `npm run deps:sync` (patch sync); policy written into `STANDARDS.md §11`. Removed the dead `reset-project` npm script (file never existed). tsc + lint + jest clean. **JS/dev-only — no native rebuild.**
- **2026-07-03** — **Audit Phase 0 executed (same day).** (1) **Forced logout wipes the query cache** — `authRefresh.ts` 401/403 branch now calls `queryClient.clear()` (was only on the logout mutation + delete-account paths); closes the cross-account stale-cache gap (audit A2). (2) **STOMP gated on token presence** — `useRealtimeConnection` connects only when `isAuthenticated && token !== null` (boolean gate; no reconnect churn on cold boot, no socket bounce on mid-session refresh; audit A5). (3) **Deps** — `expo install --fix` (15 SDK 56 patch bumps → expo-doctor 21/21) + in-range minors (react-query/axios/i18next/react-hook-form/mmkv) + `npm audit fix` (form-data HIGH + js-yaml cleared; 12 moderates remain, all the build-time `uuid→xcode→@expo/config-plugins` chain — accepted, re-check on SDK upgrades; audit A4). (4) **Hygiene** — `useOTA` effect deps (lint fully clean), `useImagePrefetch` skips on data-saver+cellular, `ChannelCard` reuses `cacheBustUrl`, the disabled re-sign snippet moved from `useChannelsQuery.ts` to `docs/API.md`. Decisions recorded: cleartext stays for dev+preview (prod none — A3 closed won't-do), A1 stream-security validation → **v2 check**, Sentry deferred. tsc + lint clean. **JS-only — no native rebuild** (patch-level native bumps land on next regular build).
- **2026-07-03** — **Full codebase audit + doc sync (no code change).** Findings + prioritized fix plan: `.claude/docs/AUDIT-2026-07-03.md`. Doc drifts corrected in place: keychain accessibility gap 5.X.11 marked resolved (code ships `AFTER_FIRST_UNLOCK_THIS_DEVICE_ONLY`); parental-PIN wording corrected to SHA-256 hash compare (`utils/pin.ts` — the code was already hashing, the docs said plaintext); Analytics section flagged **DISABLED** (mounts commented out); CLAUDE.md plan path fixed to `.claude/docs/plan.md`. Top open items from the audit: forced-logout query-cache leak (A2), SDK 56 patch drift + npm audit chain (A4), zero tests / no CI / no Sentry / no root ErrorBoundary (D1–D3), preview cleartext ATS (A3).
- **2026-07-01** — **Prerolls ease in after a short delay (no instant snap).** Both preroll overlays — app-open (`_layout.tsx`) and channel-change (`channel/[id].tsx`) — now reveal `AD_REVEAL_DELAY_MS` (2500ms, new `constants/ads.ts`) **after their host screen has settled** (Home TV channels loaded / the channel EPG loaded) rather than the instant the ad is fetched, so the ad doesn't snap up over a freshly-drawn page (user request). New pure hook **`useDelayedReveal(ready, delayMs)`** (`hooks/`, barrel-exported) — returns `true` only after `delayMs` elapses while `ready` stays true; all state updates ride the timer (async) to satisfy `react-hooks/set-state-in-effect`. Wired as an extra `&&` term on each preroll's existing gate — the channel player stays **unmounted** during the delay too (`adPending` unchanged), so the skeleton holds the 16:9 slot and there's no autoplay leak behind the deferred overlay. **Deliberately NOT background-aware** (unlike `useNow`/`useToday`) — a 2.5s reveal firing a beat late after a background round-trip is invisible; matching that pattern would be over-engineering. Mid-rolls untouched (they fire on their own scheduler). tsc + lint clean. **JS-only — no native rebuild.** No backend/API change.
- **2026-07-01** — **Per-programme geo-blocking (live-boundary case).** Geo can now be set per *programme*, not just per channel. Closes the gap where a live stream rolling into a geo-restricted programme kept playing. (1) **`EpgItem.decision`** — the EPG **list** row now carries the SAME `decision` field the `/epg/{programId}` endpoint returns (`ALLOWED`/`GEO_BLOCKED`/…), backend-evaluated per the user's country; it's the client look-ahead (the doc on the existing `decision?` field was expanded). (2) **`useLiveProgramBlock`** (new hook, decision sibling of `useParentalGuard`'s live branch) — watches TODAY's EPG independently of the day strip, reuses `useNowProgram` for the now-airing derivation + boundary timer + foreground re-eval, and blocks the live stream when the airing programme's `decision !== 'ALLOWED'`. Folded into the channel screen's `blockedNotice`/`showBlocked` (priority: live-programme block → whole-channel `geoNotice` → playback decision). (3) **`GeoEvent.programId`** (new optional field) — a `GEO_BLOCK`/`GEO_LIFT` carrying `programId` is per-programme: `useChannelRealtime.applyGeo` sets that row's `decision` across every cached day via `setQueriesData(['channel-epg', channelId])` (no refetch); omitted `programId` stays whole-channel (overlay). Both still `invalidateQueries(['channel-playback'])` to converge. No boundary network call — the live gate re-derives from the cached flag; a re-entry / date change re-fetches the EPG already flagged. **Look-ahead only, NOT the security boundary** — hard enforcement stays CDN/signed-URL. tsc clean; the sole lint error is in `VideoPlayer.tsx` (concurrent unrelated edit). **JS-only — no native rebuild.** **Backend asks (Desktop `henri-changes2.md` §5):** add `decision` per programme on `GET /channels/{id}/epg?date=` (country-evaluated) + add optional `programId` to the `/user/queue/geo` payload. No client change needed once those land.
- **2026-07-01** — **Gold-standard cleanup pass (boot, barrels, dead code, store typing, structure, docs).** Client-only hygiene refactor — **no logic and no backend/wire change**; tsc + lint clean; each step is its own commit. (1) **Boot** — removed all temporary `[BOOT]` console diagnostics (`_layout`, `useCheckToken`, `store/storage`, `useAppStore`); **kept** the `useCheckToken` keychain `try/finally` (the real fresh-install splash-hang guard — first `expo-secure-store` read can throw while Android Keystore inits) and restored the module-wiring guard in `_layout`; the boot gate now uses `fontsLoaded || fontError` (canonical Expo `useFonts` + `SplashScreen` pattern) so a failed font can't wedge the splash; `VideoPlayer`'s playback-error `console.error` gated behind `__DEV__`. (2) **Barrels** — made the `store` / `lib` (ex-`services`) / `theme` / `utils` / `api` barrels consistent with folder contents (`utils/datetime` intentionally left out — already re-exported via `formatters`). (3) **Dead code** — deleted `OfflineBanner` + `EmptyCatchupState` (zero references) and their barrel exports. (4) **Store typing** — retyped `Toast`/`Parental`/`Player` slices from self-typed `StateCreator<XSlice>` to the canonical `StateCreator<AppStore, [], [], XSlice>` (matching the other 6 slices), which removed the `as any` cast on `createPlayerSlice` in the store composition. (5) **Structure** — merged `src/config/` (literals only: `REFRESH_TOKEN_KEY`, `LINKS`) into `src/constants/`; renamed top-level `src/services/` → **`src/lib/`** (keychain + tokenVault are platform/security infra; the name collided with `api/services/` domain calls). (6) **Docs** — repointed current-state path references here + in `CLAUDE.md` / `STYLE_GUIDE.md`, and corrected README/CLAUDE env + Sentry drift (only `EXPO_PUBLIC_API_MODE` is read; base URL is hardcoded in `src/api/client.ts`; **Sentry is planned/not-installed**; MMKV is intentionally unencrypted so there is no `MMKV_ENCRYPTION_KEY`). Historical update-log / plan entries below intentionally keep their original `services/` / `config/` paths (point-in-time records).
- **2026-06-30** — **"Remember me" — refresh-token persistence is now a user choice (login + register).** New **`services/tokenVault.ts`** is the single owner of the refresh token: `setRefreshToken(token, { remember })`, `getRefreshToken()` (memory-first → keychain), `rotateRefreshToken()`, `clearRefreshToken()`. Every refresh-token read/write was rerouted through it — `useCheckToken`, `authRefresh`, `useLoginMutation`, `useLogoutMutation`, `useChangePasswordMutation`, register-verify (`register.tsx`), and `store.logout` — so `REFRESH_TOKEN_KEY` is now referenced only by `config/auth.ts` (the constant) + the vault. A **"remember me"** checkbox (zod `rememberMe: z.boolean()`, **default ON**) was added to the login form (industry-standard row with the forgot-password link) and the register form (at the end); the login mutation takes it as a stripped `LoginVariables.rememberMe`, register carries it in screen state from step 1 → the verify step (never in the `/auth/register` body). **On** = keychain (prior behavior); **off** = in-memory only → next app launch starts fresh. Behavior for the default (ON) path is byte-for-byte identical to before. The **choice** (not the token) is persisted to MMKV via new `SettingsSlice.rememberMe` (default ON, in `partialize`) and pre-fills the checkbox on both forms (login saves on submit, register on step-1 submit). i18n `auth.register.remember_me` added (en + sq; `auth.login.remember_me` already existed). New **Remember me** subsection + Persistence-boundaries row + Auth-flow steps (boot/login/logout/change-password) synced; `CLAUDE.md` storage table + Auth-flow line synced. tsc + lint clean. **JS-only — no native rebuild** (no new native deps; uses the existing keychain wrapper). No backend/API change (the flag is a pure client persistence choice).
- **2026-06-30** — **Real-time/ads refactor: pure scheduling core extracted, AdOverlay self-reports impressions, DRY invalidation.** Clarity/reuse pass — no behavior, logic, or endpoint change (tsc + lint clean). (1) **Pure mid-roll core → `src/realtime/midroll.ts`** (`selectDueMidroll` / `nextMidrollBoundaryMs` / `midrollFireMs` / `midrollLapsed`): the scheduling rules moved out of `useChannelRealtime` into a React-free, unit-testable module (exported via the `@/realtime` barrel). The hook is now a thin shell that owns only reactive state (clock, fired-set, session anchor, geo, subscriptions) and delegates every timing decision — the `dueAd` memo and boundary-timer effect are each one call now. (2) **`AdOverlay` self-reports its impression** (VAST/IMA convention — the ad unit beacons itself): removed the `onImpression(watchedSeconds)` callback and the **three near-identical reporter blocks** at the call sites (`_layout` app-open, channel screen channel-change + mid-roll). `AdOverlay` now takes `placement` (+ optional `channelId`) and fires `reportAdImpression(creative.id, { watchedSeconds, durationSeconds, channelId, placement })` itself on completion (same POST, same once-guard, same fire-and-forget). `reportAdImpression` import dropped from both call-site files. (3) **`reconcilePlayback` helper** in the hook de-dups the `invalidateQueries(['channel-playback'])` shared by the geo handler + reconnect reconciler. Net: smaller hook, testable scheduling, zero impression duplication. CLAUDE.md (Real-time files + Ads impression line) synced.
- **2026-06-30** — **Ad robustness: app-lock failsafe + mid-roll show-once dedup.** Two fixes after on-device testing (tsc + lint clean; no API change). (1) **App-lock failsafe (`AdOverlay.tsx`).** A mid-roll with a missing/`0`/`NaN` `durationSeconds` froze the auto-dismiss countdown (`deadline → NaN → isDone` never true); on a NON-skippable creative with no skip button and `onRequestClose` gated off, the app was **fully stuck**. Added `safeDuration()` clamping at the top of the overlay: `durationSeconds` falls back to 15s (cap 60s) and `skipAfterSeconds` to 5s (≤ duration) whenever the backend value is absent/0/NaN — so the ad **always** self-dismisses within a bounded window regardless of payload. The watched-time clamp uses the sanitized duration too (no `NaN` impression). (2) **Mid-roll show-once dedup (`useChannelRealtime.ts`).** `firedIds` is per-mount, so a mid-roll re-fired on every channel re-entry — worst for a fire-now ad (null/unparseable `startTime`) that the session-window guard can't filter. Added a **module-level `shownMidrollIds`** set (app-session scoped, resets on restart): `firedIds` seeds from it on mount, `onAdComplete` records into both, and an ADD/UPDATE/REMOVE op clears it (a reschedule re-arms). Ads deliberately do **not** get geo-style invalidation-on-event — the data is already cache-sourced + reconnect-reconciled; the missing piece was idempotency (show each once), a client concern, not a server round-trip. Backend ask folded into `henri-changes2.md`: send valid `durationSeconds`/`skipAfterSeconds`, and absolute `startTime` so the session-window (not the show-once fallback) does the de-duping.
- **2026-06-30** — **Real-time correctness: geo unified with the decision, reconnect reconciliation, mid-roll replay guard.** Three fixes in `useChannelRealtime.ts` (tsc + lint clean; no backend/API change — backend verification asked, see Desktop `henri-changes2.md`). (1) **Geo lift is now real-time + cache-correct.** Every `GEO_BLOCK`/`GEO_LIFT` for the current channel now `invalidateQueries(['channel-playback'])` in addition to setting/clearing the instant `geoNotice` overlay, so the socket converges with the authoritative `decision` instead of being a parallel state that, once cleared, fell back to a stale cached `GEO_BLOCKED`. Also coerces `Number(ev.channelId)` defensively. Fixes the user-observed bugs: admin removes geo → not reflected live, and leave/return within the 5-min cache window stayed blocked. (2) **Reconnect reconciliation.** `/user/queue/geo` + `/topic/channel.{id}` are pub/sub with no replay and RN drops the socket on background, so events emitted while disconnected were lost. A new effect (keyed on `realtimeConnected`, skipping the first connect via `hasConnectedRef`) re-fetches the decision + ads on every STOMP reconnect to self-heal. (3) **Mid-roll replay fixed.** `firedIds` resets on unmount, so a past-`startTime` mid-roll re-fired on channel re-entry. Replaced the `0`/epoch "fire-now" sentinel with a `null` sentinel (`midrollFireMs`) and added a **session-window guard**: a mid-roll fires only if its `startTime` ∈ `[sessionStart, now]` (the wall-clock captured at hook mount = one channel visit) — a start that passed before arrival is never inserted into the past. `null`/unparseable start still = fire-now (defensive). `henri-changes2.md` (Desktop) documents the full client socket flow + the backend verify checklist (emit `GEO_LIFT`, `GET /channels/{id}` freshness, absolute ISO `startTime`, consistent `channelId` JSON type).
- **2026-06-30** — **Mid-roll ads pause the content stream (+ PiP-entry gate).** Closed the gap where a mid-roll overlaid a *still-playing* live/recorded stream (the preroll already unmounted the player; the mid-roll did not). New ad-driven **`paused` prop** on `VideoPlayer` (+ threaded through `LivePlayer`): an effect reconciles it onto the imperative `expo-video` player (`player.pause()` / `player.play()`) with **no remount** — the Expo-idiomatic declarative-state→imperative bridge, and the CSAI / Google-IMA `ContentPauseRequested`/`ContentResumeRequested` model. The channel screen drives it with `adActive = !!midrollAd` → `paused={adActive}`. **Resume branches on `player.isLive`:** live best-effort re-syncs to the live edge (`seekBy(player.currentOffsetFromLive)`; skipped → resume-in-place when the manifest carries no `EXT-X-PROGRAM-DATE-TIME` so the offset is null) so a break doesn't strand the viewer behind live; recorded resumes in place. The same flag flips `allowsPictureInPicture`/`startsPictureInPictureAutomatically` → `!paused`, gating PiP **entry** off during an ad (an already-active PiP is **not** ejected — pausing leaves a frozen frame, which beats a window vanishing while the user is in another app; ejection *is* possible via the `VideoView` ref's `stopPictureInPicture()`, deliberately not used). Preroll unchanged (still unmounts — strictly better than mount-and-pause on first open: no wasted CDN session). Two guards: the source-swap autoplay re-play is gated `&& !paused` (a quality switch mid-ad can't un-pause), and the setup-callback autoplay is `autoPlay && !paused`. Live-edge seek uses a **method call** (`seekBy`) not a property write (`targetOffsetFromLive`) — the latter trips `react-hooks/immutability` (React Compiler treats the hook-returned player as frozen) and is iOS-only anyway. **Verified against installed expo-video 56.1.2 types** (`pause`/`play`/`seekBy`/`isLive`/`currentOffsetFromLive`; `VideoView` PiP props + `startPictureInPicture`/`stopPictureInPicture` ref methods). **Open caveat:** the live-edge seek depends on the RTSH origin emitting `EXT-X-PROGRAM-DATE-TIME` — validate on a real live stream (ties to the untested-AES `TODO(anx 2026-06-02)`). tsc + lint clean. **JS-only — no native rebuild** (props/methods already in the shipped binary). No backend/API change.
- **2026-06-29** — **Auto-rotation removed — app is portrait-only; only the player rotates to landscape, button-driven.** Supersedes the 2026-06-23 "fullscreen is orientation-driven (landscape ⇔ fullscreen)" behavior. (1) **`useFullscreenOrientation` is now state-driven, not sensor-driven** (`hooks/useOrientation.ts`): `isFullscreen` is local `useState`; `enterFullscreen` locks `LANDSCAPE` + sets true, `exitFullscreen` locks `PORTRAIT_UP` + sets false (cleanup restores portrait if the player unmounts while fullscreen). Physically rotating the phone no longer does anything — fullscreen happens **only** when the user taps the bottom-right expand control (`PlayerControls` → `onToggleFullscreen` → `toggleFullscreen`). The old sensor machinery (`useOrientation` `useSyncExternalStore` + `addOrientationChangeListener`, the sticky-`unknown` logic, the 1.5s re-arm) was deleted. (2) **New `useLockPortrait()`** (same file) locks the app to `PORTRAIT_UP` at runtime; mounted once in `RootLayoutNav` (`_layout.tsx`) — this is what kills whole-app auto-rotation while leaving the player free to lock landscape on demand (last-writer-wins). (3) **TV/tablet untouched:** every lock no-ops on `Platform.isTV` (TV runs landscape natively, no portrait concept), so the large-screen layout + the `@/responsive` grid are unaffected; tablet stays portrait (its portrait grid still renders — the landscape-grid branch simply isn't reached, by design). (4) **`app.config.ts` keeps `orientation: 'default'` ON PURPOSE** (commented) — iOS must still declare the landscape interface orientations for the player's `lockAsync(LANDSCAPE)` to work; the portrait default is enforced at runtime, not by the manifest. **JS-only — no native rebuild** (expo-screen-orientation already shipped; `orientation: 'default'` unchanged). tsc + lint clean. No backend/API change.
- **2026-06-29** — **Real-time layer (STOMP over WebSocket) — presence, watch-time, mid-roll ads + geo.** New `src/realtime/` module (`@/realtime`) over `@stomp/stompjs`: `events.ts` (destinations + `MidrollEvent`/`GeoEvent`/`WatchKind`), `client.ts` (singleton, JWT on CONNECT, writes `RealtimeSlice.realtimeConnected`), `useRealtimeConnection` (app-level connection = presence, mounted in `(app)/_layout`), `useChannelRealtime` (per-channel: channel-topic + user-geo-queue subscribe, watch-segment publish, mid-roll scheduler, geo notice). **Ads reworked to a merged array** — `GET /ads?channelId=` → `Ad[]` (preroll + mid-rolls) / `GET /ads` → APP_OPEN; new `Ad` type (extends `AdCreative` + `placement` + absolute `startTime` + `validUntil`), `getAds`/`useAdsQuery` replace `getAd`/`useAdQuery` (both removed, plus dead `LaunchAdHost`). Mid-roll = **Ads Option A** (client-scheduled, data single-sourced in the `['ads', channelId]` cache via `setQueryData`, due ad derived purely + one boundary timer like `useNowProgram`); **impressions** reported on show via new `AdOverlay.onShown` → `reportAdImpression` (`POST /ads/{id}/impression`, fire-and-forget) across all placements. **Geo = Option B** (backend-fired `GEO_BLOCK`/`GEO_LIFT` on `/user/queue/geo`, folded into the channel screen's existing decision-blocked UI). New `createRealtimeSlice` (runtime, not persisted). Mock `GET /ads` returns the merged `Ad[]` + `POST /ads/{id}/impression` → 204. Full section above; backend contract `docs/REALTIME_SOCKET.md` (+ Desktop `henri-changes.md` for review). tsc + lint clean. **Backend STOMP server + merged-endpoint changes pending Henri's validation** (esp. absolute `startTime`). `/refresh` playback re-sign deliberately untouched (separate concern).
- **2026-06-28** — **Playback re-sign disabled for the moment — `useChannelPlaybackQuery` reverts to global config.** Removed the pre-expiry interval re-sign from `useChannelPlaybackQuery` (`queries/useChannelsQuery.ts`): dropped the `refetchInterval` (armed at `expiresAt − 30s`, 5s floor), `refetchIntervalInBackground: false`, the cached-`sessionId` branch, and the `refreshPlayback` / `queryClient` imports + `PLAYBACK_REFRESH_LEAD_MS`/`_FLOOR_MS` constants. The `queryFn` is now a plain one-shot decision fetch (`getChannelById` live / `getCatchupPlayback` recorded) and the query inherits the **global config** (`staleTime: 5min` + refetch-on-focus/reconnect) — **no `POST /channels/playback/refresh` call**, no interval. `expiresAt`/`sessionId` in the `PlaybackDecisionDTO` are no longer consumed by the client. **Left intact for easy re-enable** (per user, "for the moment"): the `refreshPlayback` service, `CHANNELS_ROUTES.PLAYBACK_REFRESH` endpoint, and the mock POST handler — re-enable by reinstating a `refetchInterval`. Supersedes the 2026-06-25 re-sign + the 2026-06-24 staleTime-margin entries below (the 5min<6min-TTL margin reasoning is moot while there's no interval). `docs/API.md` Channels section (re-sign subsection + `expiresAt`/`sessionId` note) synced; JSDoc on `useChannelPlaybackQuery` rewritten. tsc + lint clean. No backend/API change. **Open risk:** with no re-sign, a signed stream URL that expires mid-watch (~6min TTL) will 403 on the next segment until something re-fetches the decision (mount/focus/reconnect or a >5min-stale serve) — acceptable interim while the signed-URL contract is finalized with the backend.
- **2026-06-27** — **Custom per-platform media `User-Agent` (stream-request gating, interim pre-AES).** New `getStreamUserAgent()` + cached `getStreamHeaders()` in `utils/device.ts` stamp a `User-Agent: RTSHTani-<Platform>` header (e.g. `RTSHTani-AndroidTV`; no version) on the **media engines** (NOT `apiClient`): wired to expo-video via `LivePlayer`'s `streamHeaders` (`channel/[id].tsx`, live + recorded) and to expo-audio via `RadioAudioHost`'s `player.replace({ uri, headers })` (radio). Platform label is an exhaustive `Record<DeviceType, …>` (`STREAM_UA_LABEL`) so it never diverges from the device registry; the header object is cached for a stable reference (no re-render / source-replace churn). Lets the origin/CDN recognize app traffic and reject a manifest URL pasted into a browser (which carries the browser's own UA). Verified against the **installed native code**: expo-video Android `setUserAgent()` / iOS `AVURLAssetHTTPHeaderFieldsKey`; expo-audio Android `setDefaultRequestProperties` / iOS same key. **Spoofable speed-bump, not access control** — real lock stays signed/expiring/IP-bound URLs (still pending from origin/middleware; this is the interim while AES-128 key generation isn't ready). **iOS asterisk:** UA-override uses the *private* asset header key (the public `AVURLAssetHTTPUserAgentKey` iOS 16+ isn't used by expo-video) — verify it reaches the origin; fallback is a custom `X-Client-Platform` header (one line in `getStreamHeaders()`). Tizen/webOS (separate apps) mirror the labels via their own UA APIs. Raw UA list handed to the backend team. Device-identity section + `CLAUDE.md` Open-risk note synced. tsc + lint clean. **JS-only — NO native rebuild** (the header-handling native code already ships in the installed expo-video/expo-audio binaries; the change is pure JS data passed across the bridge, so it lands on a Metro reload / OTA). A rebuild would only be needed to switch players or add a config plugin. **Open choice (backend team):** `User-Agent` (nginx logs it free, but iOS override is flaky via the private asset key) vs a custom **`X-Client-Platform`** header (more reliable on iOS — a pure add, no reserved-header fight — but the backend must add a log/gate rule). Same spoofability either way; switching is the single `STREAM_ID_HEADER` constant (`'User-Agent'` | `'X-Client-Platform'`) in `utils/device.ts` — call sites unchanged. No backend/API change.
- **2026-06-26** — **`useMeQuery` → fetch-once + explicit invalidation (supersedes cross-device foreground sync).** `useMeQuery` now overrides the global 5-min/refetch-on-focus defaults with `staleTime: Infinity` + `gcTime: Infinity` + `refetchOnWindowFocus: false` + `refetchOnReconnect: false`, so it fetches `GET /users/me` **exactly once per authenticated session** (on `enabled` flipping true, mounted once in `(app)/_layout.tsx`) and the **only** refresh path is explicit `queryClient.invalidateQueries({ queryKey: ['me'] })` — to be fired from any future profile-mutating mutation (none exists today; `useChangePasswordMutation` leaves `user` untouched). **Trade-off (user decision):** drops the prior foreground/reconnect/5-min-poll cross-device profile sync (§5c) — a profile/parental change made on another device no longer auto-arrives on foreground; it lands on next cold boot or local invalidation. Auth-flow §5c + the `useMeQuery` JSDoc synced. tsc + lint clean. No backend/API change.
- **2026-06-26** — **`useDeviceIdentity` moved from the Home screen to `(app)/_layout`.** Co-located the device-registration upsert next to `useMeQuery` in the authenticated layout (removed from `(app)/(tabs)/index.tsx`). Both are "we're now authenticated" effects; the layout mounts on *any* authenticated entry, so registration now fires even on a deep link straight into a non-Home tab (the Home mount delayed it until Home rendered). Still a one-shot fire-and-forget `mutate()`; no logic change. `useDeviceIdentity` JSDoc + the Device-identity section ("mounted on the Home screen" → layout) + `CLAUDE.md` networking line synced. tsc + lint clean. No backend/API change.
- **2026-06-26** — **Analytics & telemetry (first-party, spec MW.14 / Mon.6).** New self-contained `src/analytics/` module emitting standardized events to `POST /analytics/events` (own backend, no 3rd-party SDK). Two primitives sharing one enrichment helper + one service: a plain fire-and-forget **`track(event, props)`** for discrete imperative events (returns `void`, bypasses the global `QueryCache`/`MutationCache` error modal, no PII by type) and a **`useQuery` heartbeat** (`refetchInterval: 5min`, background-paused via the existing `focusManager` bridge) for periodic liveness. Single **`useAnalytics()`** hook (mount once in `(app)/_layout`) owns `app_open` (once/process) + session_start/end (foreground-paired, via `useAppState`) + the heartbeat; separate **`useWatchTracking(id, kind)`** for per-screen `channel_watch_start/_end` (channel screen `'live'`/`'recorded'` + `RadioAudioHost` `'radio'`, keyed on `radioChannelId` so it spans navigation). `stream_error` via a new `LivePlayer onError` prop. New `settings.analyticsEnabled` opt-out (persisted; Settings toggle; gates `track` + the query's `enabled`). New `ANALYTICS_ROUTES.EVENTS` + `services/analytics.ts` + mock `POST /analytics/events` → 202. i18n `settings.analytics.*` (en + sq). Decision trail: discrete events are imperative writes (mutation/query both wrong → plain fn); heartbeat is a poll (query, reusing TanStack's interval + focusManager). Full rationale + wiring table + known gaps: **Analytics & telemetry** section above. tsc + lint clean. **Backend contract assumed** — confirm event names/payload with middleware.
- **2026-06-25** — **Guide snapshots render again (same cache-bust logic as Home).** `GuideRow` drew its live snapshot tile with `cachePolicy="none"`, which on expo-image 56 leaves the image **unpainted** — so Guide thumbnails were blank while Home (already migrated) and Search (plain `disk` default) showed. Fixed by reusing Home's pattern: dropped `cachePolicy="none"`, and cache-bust the stable-URL/mutable-content snapshot with a `?cb=<dataUpdatedAt>` query param (`disk` cache keeps it painted; a new token on each refetch forces a fresh frame). New pure helper **`cacheBustUrl(uri, token)`** (`utils/image.ts`, exported via the utils barrel) holds the URL-append logic; `GuideRow` gained a `thumbnailRefreshKey` prop, the Guide screen threads the **guide query's own `dataUpdatedAt`** (`useGuideQuery` now returns it). **Queries stay separate** (per user, 2026-06-25): `useGuideQuery` (`/guide`) is its own endpoint and is NOT merged into `['channels']`, so Guide snapshots refresh on Guide's own triggers (tab focus / pull / foreground), not a Home refresh. `ChannelCard` left untouched (its inline cache-bust still works; not DRY-ed to avoid churning working code). Search left as-is (out of scope; `disk`-cached, no cache-bust). tsc + lint clean. No backend/API change.
- **2026-06-25** — **Playback session re-sign via `POST /channels/playback/refresh` (dedicated endpoint, replaces re-decision refetch).** The pre-expiry re-sign previously re-ran the full decision endpoint (`GET /channels/{id}` / `getCatchupPlayback`) on every `refetchInterval` tick — re-running geo/device-class/decision and minting a new session each time. Replaced with a session-scoped re-sign: new `refreshPlayback(sessionId)` service → `POST /channels/playback/refresh { sessionId }` (new `CHANNELS_ROUTES.PLAYBACK_REFRESH`), returning the same `PlaybackDecisionDTO` shape (fresh signed `streams` URL + new `expiresAt`, **same session, no geo re-check** — the `decision`/geo gate is evaluated only on the initial open). `useChannelPlaybackQuery`'s `queryFn` now branches on the cached `sessionId` (`queryClient.getQueryData`): present → `refreshPlayback`; absent (first open, or after the live key is invalidated on return-to-live) → the initial GET decision endpoint (`getChannelById` for live, `getCatchupPlayback` for recorded). The `expiresAt − 30s` interval, 5s floor, and `refetchIntervalInBackground: false` are unchanged. Initial open stays a **GET** (per user, 2026-06-25); only the refresh is a POST. Mock POST handler added (`/channels/playback/refresh`, echoes the posted `sessionId`). `docs/API.md` Channels section updated (new endpoint subsection + the "sessionId/expiresAt not yet consumed" note corrected). tsc + lint clean.
- **2026-06-25** — **Seek bar actually moves: `timeUpdateEventInterval` set; live kept non-seekable on purpose.** Two coupled fixes after on-device testing of the new scrubber. (1) **Root cause of the dead recorded bar** — `VideoPlayer` registered a `timeUpdate` listener but expo-video emits that event **only when `player.timeUpdateEventInterval > 0`** (default `0` = never). So `currentTime`/`duration` stayed `0` forever: the recorded bar's `isSeekable = duration > 0` never flipped true (pinned at 0, non-draggable) and the fill never advanced. Live *masked* it because its progress is hardcoded to the edge. Fixed with one line in the `useVideoPlayer` setup callback — `p.timeUpdateEventInterval = 0.5` — so `timeUpdate` fires twice a second and both the fill and seekability come alive for recorded/catch-up. (2) **Consequence for live** — with the event now firing, the live DVR window reports a *finite* duration, which would have made live scrubbable too. Per product decision (2026-06-25) **live stays pinned full at the live edge and non-seekable**, so `isSeekable` was tightened from `duration > 0` to **`!isLive && duration > 0`** (exclude live by flag, not by duration). Recorded unchanged (draggable from 0); live unchanged from the user's perspective (full bar at edge). Supersedes the "`PlayerControls.isSeekable = duration > 0`" wording in the 2026-06-22 + 2026-06-25-scrubber entries below. `CLAUDE.md` PlayerControls + LivePlayer lines synced. tsc + lint clean. No backend/API change.
- **2026-06-25** — **Draggable seek scrubber (recorded/catch-up) — `Gesture.Pan` + Reanimated.** Closed the "can't drag to seek on recorded programmes" bug: the `PlayerControls` seek bar handled only `onTouchStart` (tap-to-jump), never a drag. Rebuilt it as a proper scrubber on the Expo-recommended stack — `react-native-gesture-handler` `Gesture.Pan()` driving Reanimated shared values on the UI thread (60fps, no JS round-trip per frame). One Pan gesture does both tap-to-jump (onBegin seeds position) and drag (onUpdate follows the finger); `onFinalize` commits via `scheduleOnRN(commitSeek)` (the RN-worklets replacement for the now-deprecated `runOnJS`). `enabled(isSeekable)` so it's active only when `duration > 0` (recorded/catch-up) and inert at the live edge — **live behavior unchanged** (full bar pinned to edge, non-seekable). On release the bar **holds at the released position** until the mirrored progress catches up (worklet-owned `holdSv`/`heldRatioSv`), avoiding a one-frame snap-back before the next `timeUpdate`. `commitSeek` uses `player.seekBy(target − currentTime)` not the `currentTime` setter (assigning a player prop trips react-hooks/immutability). **Required `GestureHandlerRootView` at the app root** (`app/_layout.tsx`) — the app had none (no prior `GestureDetector` usage). Key constraint learned: the React-Compiler `react-hooks/immutability` rule freezes any SharedValue the effect touches (and any JS-written SV read in `useDerivedValue`), so each SV has a single mutation owner — `progressSv` effect-owned (read-only in worklets), the hold pair worklet-owned, and the scrub position stored as a 0..1 ratio so `displayProgress` never reads the JS-written `trackWidth` (only the gesture worklets + animated styles do). `CLAUDE.md` PlayerControls line synced. tsc + lint clean. No backend/API change.
- **2026-06-25** — **Delete account (`DELETE /users/me`).** Added permanent account deletion next to Logout on the Profile screen. New `deleteAccount()` service (`services/users.ts`, no body — the access token identifies the account) + `useDeleteAccountMutation`. Unlike logout it is **not** best-effort: the local wipe runs **only on a confirmed 200** (`onSuccess`, not `onSettled`) so a failed delete keeps the user signed in (failure falls through to the global `apiError` modal — no error-suppressing `meta`). The wipe is `store.logout()` + **`clearParentalConfig()`** + `queryClient.clear()`. New `ParentalSlice.clearParentalConfig()` action (resets `parentalEnabled`/`parentalPin` + lockout to defaults) — account deletion is the **only** path that clears the device-level parental gate; plain logout still leaves it intact (per the user, 2026-06-25). Profile screen reuses the existing `confirmation` modal (destructive button) before deleting. New i18n `profile.delete_account` / `delete_confirm_title` / `delete_confirm_message` (en + sq); mock `DELETE /users/me` → 200 handler. Auth-flow step **5a** + Parental-control "one exception clears it" note + `docs/API.md` (`DELETE /users/me` section) + `CLAUDE.md` feature list synced. tsc + lint clean.
- **2026-06-24** — **Channel screen survives midnight (`useToday` day anchor).** Closed the stale-schedule gap: the catch-up day strip + selected-day were anchored to a **mount-time `new Date()`** (`days` useMemo + initial `selectedKey` in `channel/[id].tsx`), so an app left backgrounded past midnight came back showing **yesterday's** lineup — the EPG refetch-on-foreground (5min staleTime + focusManager) didn't help because the stale date is baked into the query key (`['channel-epg', id, selectedKey]`), so it just re-pulled yesterday. Root cause was the *anchor*, not the fetch. New **`useToday()` hook** (`hooks/`, in barrel) returns the local `YYYY-MM-DD` day key kept correct across midnight via **two triggers**: a single `setTimeout` to the next local midnight (rolls while foregrounded — late-night live viewing; re-armed each flip) **and** app foreground via `useAppState` (RN throttles backgrounded timers, so the timer alone can't be trusted — the common overnight-background case). Day-key held in **state** (re-renders consumers on flip; setting the same key is an `Object.is` no-op so a pre-midnight foreground costs nothing) — mirrors `useNow`/`useNowProgram`. `channel/[id].tsx`: `days` now anchors on `todayKey` (deps `[todayKey, formatDate, t]`); the local `toDateKey` was **extracted to `utils/datetime.ts`** (exported, shared by the hook). **Selection-follow** uses React's adjust-state-during-render pattern (a `dayAnchor` state + an `if (todayKey !== dayAnchor)` block — no effect, no extra commit): the selection rolls forward to the new today **only if the user was parked on today**; a deliberately-chosen past/future day stays put (the chip just relabels as the strip shifts). When it advances, `useChannelEpgQuery(newKey)` refetches and `selectedDay.isToday` flips true so `useNowProgram` gets the fresh schedule — no other change. Guide/Search are unaffected (server-driven `now` / server-computed `today`, no client day anchor). Accepted edge: parked on the oldest day (−7) at rollover drops off the new 15-day window → falls back to oldest. tsc + lint clean. No backend/API change.
- **2026-06-24** — **Query freshness unified on a 5min/15min global; playback `staleTime: Infinity` dropped.** Replaced the global TanStack default `staleTime: Infinity` / `gcTime: 1h` (`client.ts`) with **`staleTime: 5min` / `gcTime: 15min`**, then removed the now-redundant per-query overrides that previously bumped off `Infinity` (`useEpgQuery`, `useChannelEpgQuery`, `useChannelsQuery` list). `useAdQuery` keeps its explicit `staleTime: 0 / gcTime: 0` (always fresh) — now an override of 5min rather than `Infinity`. **`useChannelPlaybackQuery` lost its `staleTime: Infinity`** and now inherits the 5min global: since the signed stream URL has a **fixed ~6min `expiresAt` TTL**, `staleTime (5min) < TTL (6min)` guarantees a still-"fresh" cache entry (age < 5min) always has ≥1min of token life left — so a non-stale cached URL is never expired. Re-entering the screen / reconnecting on a short blip serves the valid cache with **no needless rebuffer**; a stale entry (>5min, e.g. after a long outage) auto-refetches a fresh URL on mount/focus/reconnect. The proactive `refetchInterval` (re-sign at `expiresAt − 30s`, `refetchIntervalInBackground: false`) is unchanged — it covers steady-state playback. Earlier the `Infinity` was kept to stop incidental focus/reconnect refetches from swapping the source mid-watch; the 1-min margin now makes that safe without it. Rationale: the fixed-staleTime approach is correct **only because** the token TTL is known and comfortably exceeds 5min (a shorter/unknown TTL would have required a dynamic `staleTime = expiresAt − now − 30s`). JSDoc on `useChannelPlaybackQuery` rewritten to document the margin. tsc + lint clean. No backend/API change.
- **2026-06-23** — **Device info out of request headers; registration → mutation on Home; `deviceClass` → playback query param (user decision, temporary).** Rewrote the **Device identity & request headers** section. (1) Deleted the header stamp (`buildDeviceHeaders`, `getDevicePlatform`, `DEVICE_HEADERS`) — `X-Device-Id`/`X-Device-Platform`/`X-App-Version` are no longer sent; `Authorization` + `Accept-Language` are the only headers. (2) `useDeviceIdentity` moved from `(app)/_layout.tsx` to the **Home screen** and now fires `useRegisterDeviceMutation` (new) once on mount — replaced the hand-rolled `useEffect`/`try-catch`. (3) New **`SILENT_ERROR`** meta in `client.ts` (full-suppress the global modal at any status; the existing `INLINE_CLIENT_ERROR` only mutes 4xx) — used by the registration mutation. (4) New **`DeviceClass` (`MOBILE|TV|STB`)** + `getDeviceClass()` (derived from `getDeviceType()`); sent as a **`deviceClass` query param** on `getChannelById` + `getCatchupPlayback` so the backend picks a platform-specific player URL — *not* in the registration body (multi-device + race rationale in the section). Added a **"Divergence from the header spec"** subsection (426 per-request gate now inert; no per-request device correlation). `docs/API.md`, `CLAUDE.md`, `docs/plan.md` synced.
- **2026-06-01** — Initial doc. Captures state after Phase 5.5a audit fixes + Phase 5.8 `useBootstrap` (offline-first boot).
- **2026-06-09** — Added **Radio audio** section: store-driven `RadioAudioHost` above the router (22.11) — playback now survives navigation; routes/mini-player only mutate `PlayerSlice`.
- **2026-06-10** — Auth-flow review (22.14d): documented the **dual-token rationale** (why access+refresh even with always-on sessions), added **Zod at the auth boundary** + the `getMe` envelope fix, and added the **Parental control** section. Refreshed the stale "PIN absent" / "no Zod" gaps. **Decided 5.X.10: MMKV encryption is an accepted risk (won't encrypt)** — secrets are keychain/memory-only; field-whitelist (5.X.17) is the chosen lightweight guard.
- **2026-06-10** — Parental **live re-check (22.14c)**: added `useLiveParentalGuard` (boundary-timer + foreground re-eval) to the Parental control section; live programme transitions now re-gate. Closed the "live re-check" gap; added a VOD-program-gate follow-up note.
- **2026-06-12** — Added **Device identity & request headers** section (11.X.10, backend header spec 2026-06-11): keychain `X-Device-Id`, build-time STB platform override, 426 `forceUpdate` blocking modal, unauthenticated `/app/version` gate. Contract seeded into `docs/API.md`.
- **2026-06-12** — **Device registration (11.X.11):** `POST /devices` upsert (`deviceKey` = `X-Device-Id` UUID, expo-device form-factor enum) fired from `useDeviceIdentity` whenever `isAuthenticated` flips true — login, register completion, cold boot with session. Fire-and-forget, failures swallowed. Contract + open questions added to `docs/API.md`.
- **2026-06-12** — **Splash redesign:** native splash now shows the logo from frame zero (iOS lockup / Android square mark per the 12+ circle constraint), superseding the transparent-icon decision of 2026-06-11; `splash-transparent.png` removed. Rewrote the Boot/Splash section with the three-phase handoff (native → `BrandedSplash` → router).
- **2026-06-12** — **Auth reconciled against the live swagger** (`/v3/api-docs/end-user`; decisions agreed with backend): `/api/v1` base prefix; refresh returns access token only (**no rotation** — superseded the rotating-refresh rationale; no user in the response → manual-wipe recovery always hits `getMe`); logout POSTs `{ refreshToken }`; register is single-shot (all profile data + `acceptTerms`) → OTP verify auto-login; password reset rides a one-time `resetToken`, success replaces the route to login; `UserDTO` validated+mapped via `userDtoSchema` transform; `/users/me` is bare (no envelope); `DeviceType` enum confirmed from `DeviceInfoDTO`; mock server now mirrors the wire contract, also intercepts `refreshClient`, and rejects 4xx like real axios. Contract table: `docs/API.md → Authentication`.
- **2026-06-12** — **Refresh hardening (auth-flow trace review):** single-flight promise moved from the 401 interceptor into `refreshAccessToken` itself, so the interceptor and the boot background refresh share one in-flight request (concurrent refreshes were harmless only because the token is static). Fixed the interceptor logging out on *any* null refresh result — transient failures (offline/5xx) during a 401-retry no longer wipe the session; logout fires only inside `refreshAccessToken` on confirmed 401/403. Boot background refresh is now skipped when the manual-wipe recovery path already hydrated the token (was a duplicate round-trip).
- **2026-06-12** — **Second swagger pass (auth scope):** register wire field is `termsAccepted` (client keeps `acceptTerms` in the form/schema; mapped at the service boundary next to the gender transform). Device registration moved to **`PUT /users/me/device`** with a **bare `DeviceInfoDTO`** body (was `POST /devices` + `{ device }` envelope) — backend confirmed the optional `device` field in the login/register-verify bodies is unnecessary for this client. `DeviceType` enum confirmed to match the client exactly. Mock handler + `docs/API.md` synced.
- **2026-06-15** — **Parental control simplified (product decision):** the PIN is content gating, not a credential, so it now rides on the **user object** (`user.parentalPin = { enabled, pin }`, persisted in plaintext MMKV) instead of a keychain `SHA-256` verifier + server KDF. Verify is a local string compare; gating keys on `enabled`; the keychain cache, `hashPin`/`verifyPin` usage, `verify-pin`/`DELETE` endpoints, and the `isPinSet`/`setIsPinSet` slice state were removed. v1 wires **`POST /parental` only** — disable / change-PIN / forgot-PIN are deferred (Settings switch locks once enabled). Persistence-boundary invariant amended to allow the `pin` as a documented plaintext exception. `userDtoSchema`, mock handlers, and `docs/API.md` synced. `utils/crypto.ts` (`hashPin`/`verifyPin`) deleted — no consumer left. `parentalPin.pin` is `string | null` (enabled-but-unset case).
- **2026-06-17** — **Device identity mount moved to the app layout (simplification).** `useDeviceIdentity` + `useMeQuery` moved out of `useBootstrap` into `(app)/_layout.tsx` (the authenticated subtree). The hook collapsed to a single mount effect — stamp headers, then fire `registerDevice` on every app entry — dropping the `isAuthenticated` selector/guard (redundant under `(app)`). Trade-off: auth-screen requests no longer carry `X-Device-*`/`X-App-Version` (logged as a known gap). `useBootstrap` JSDoc + responsibilities trimmed; `CLAUDE.md` "stamped at boot" line updated. No backend/API change.
- **2026-06-15** — **Account self-service + cross-device sync (auth/parental/`/me`/device scope, swagger reconciliation):** (1) **Change password** — `POST /users/me/change-password` returns a **rotated** `{ accessToken, refreshToken }`; `useChangePasswordMutation` rewrites the keychain + access token; new authenticated screen `app/(app)/change-password.tsx` (RHF+zod) opened from Settings → Account; `logoutOtherDevices` flag folds in "sign out everywhere" (no separate endpoint). `authErrorMessage` extended with a `code`-keyed map. (2) **Parental disable/toggle built** — Settings switch now drives `POST /parental { enabled, pin }` (create) and `PATCH /parental { enabled }` (toggle, no `currentPin` — local verify before disable); supersedes the "switch locks once enabled" gap. (3) **Cross-device profile sync** — new `useMeQuery` + `setupFocusManager` (AppState→`focusManager`) refetch `GET /users/me` on foreground / reconnect / 5-min active poll → store, so one device's change reaches others without sockets; explicitly NOT bolted onto the 401-refresh hot path. (4) `logout` now sends `{ refreshToken, logoutOtherDevices }`; `educationLevel` mapped in `userDtoSchema`; `tokenPairSchema` added. Mocks + `docs/API.md` + `CLAUDE.md` synced. Playback/EPG/channels/ads reconciliation deferred to a later pass.
- **2026-06-16** — **Parental control → device-level, client-only (product decision):** the PIN is no longer account-level / backend-synced. It moved off the user object into `ParentalSlice` (`parentalEnabled` + `parentalPin`, MMKV-persisted via `partialize`), and **all backend involvement was removed**: deleted `setParentalPin`/`updateParental` services, `useParentalMutations`, the `PARENTAL` endpoint, the `/parental` mock handlers + `fixtures/parental.ts` + `userWithPin`, and `parentalPin` from `User`/`userDtoSchema` (the `ParentalPin` type too). `setParentalConfig` moved from `UserSlice` to `ParentalSlice`; `ParentalPinModal` set-mode now stores locally; Settings/Profile/channel read `s.parentalEnabled`; verify is `pin === parentalPin`. The PIN now survives logout/login and is intentionally **not** synced across devices (per-device by design). Supersedes the 2026-06-15 user-object model. `CLAUDE.md`, this file, and `docs/API.md` synced.
- **2026-06-17** — **Boot/splash simplified — native splash only, `useCheckToken` reduced to a keychain check.** (1) **`useCheckToken`** dropped its `getMe()` + `refreshAccessToken()` network calls, the try-catch, and the "MMKV-wiped/keychain-survived" recovery path — it now only reads the keychain and sets `isAuthenticated: true` (user re-hydration is `useMeQuery`'s job, access-token refresh is `useBootstrap`'s background refresh). `useBootstrap` no longer wraps it and returns `void`; its background refresh keys off store `isAuthenticated` (fires only when `token` is null). (2) **`_layout.tsx`** split into `RootLayout` (providers) + `RootLayoutNav` (all hooks + `Stack`) so TanStack hooks have a `QueryClientProvider` ancestor; guards key on store `isAuthenticated`. (3) **`BrandedSplash` + `app/index.tsx` deleted** — no JS splash phase / progress bar; the native splash (same RTSH lockup) holds the whole boot and is hidden only after fonts + keychain check resolve. Rewrote the Boot/Splash section + the Auth-flow boot step. No backend/API change.
- **2026-06-18** — **`useBootstrap` removed; Appearance listener extracted to `useSystemTheme`.** The root orchestrator hook is gone — `RootLayoutNav` now calls each boot hook directly (`useCheckToken`, `useNetworkMonitor`, `useSystemTheme`, `useOTA`), and the one-time wiring (`setupAuthRefresh`, `setupFocusManager`, `initI18n`) runs at **module scope** in `_layout.tsx` instead of in the render body. (1) New **`useSystemTheme`** hook owns the `Appearance.addChangeListener` (re-resolves `colors` while `mode === 'system'`). (2) The old `useBootstrap` **cold-boot background refresh was dropped** — redundant with the 401 interceptor, which already refreshes-and-retries the first authed request on cold boot. `useCheckToken` stays keychain-only (untouched). `useBootstrap.ts` deleted, removed from the hooks barrel; stale `useBootstrap` mentions updated across this file + `focusManager.ts` / `i18n` / `useMeQuery` / `useNetworkMonitor` / `authRefresh.ts` JSDoc. No backend/API change.
- **2026-06-17** — **Boot render-gate added; corrected the "synchronous keychain read" claim.** `RootLayoutNav` now `return null`s until `fontsLoaded && tokenChecked`. `useCheckToken`'s keychain read is **async**, so the old doc claim that it "resolves before first paint" was wrong — without a gate the `(auth)` group rendered during the async read and flashed login on a JS/Metro reload (cold boot was covered by the native splash). The `return null` gate holds the brand-black root window until `isAuthenticated` is known, killing the flash on cold boot *and* reload. Single source of auth truth stays the keychain (no `isAuthenticated` persistence — rejected to avoid an MMKV-vs-keychain divergence). No backend/API change.
- **2026-06-18** — **Responsive layout & sizing module (`@/responsive`) — down-payment on 22.18.** New portable, self-contained module (`react`+`react-native` only) owning all device-size decisions, split into two industry-standard concerns: (1) **layout** — `getDeviceClass` (shortest-side `sw600dp` classifier), `useResponsive`, `useResponsiveGrid` (`GRID_COLUMNS` phone 2/2, tablet 3/4, TV 4/4); the Home grid now drives `numColumns` from `useResponsiveGrid()` (replaced the per-screen `floor(width/180)`). (2) **sizing** — `scaled()` applies a discrete per-class step (`UI_SCALE` phone 1 / tablet 1.15 / TV 1.3) **at the token layer** (`FONTSIZE`, `SPACING`, `ReusableText`/`ReusableBtn` size tables), resolved once at launch; phone factor 1 ⇒ phone UI unchanged. Deliberately **not** unified with `utils/device.ts → getDeviceType()` (window vs physical device). Added the "Responsive layout & sizing" section here + `STYLE_GUIDE → Responsive Sizing`; updated the Device-identity "layout is not static" note. No backend/API change.
- **2026-06-17** — **Global API error handling + session-expired notice.** Added a `QueryCache`/`MutationCache` `onError` to the `queryClient` (`client.ts`): unexpected query/mutation failures (5xx, network, 404…) now open the `apiError` modal once from one place (query → Retry/`fetch()`, mutation → dismiss), replacing the silent data-screen gap. It skips 401/403/426 (interceptor-owned) and any `meta: SKIP_GLOBAL_ERROR` (new exported const) — set on the login/change-password/register/reset mutations, which render their errors inline. v5-idiomatic (per-`useQuery` `onError` was removed in v5); chosen over the Solitar per-hook `useEffect(error→modal)`. Separately, `refreshAccessToken`'s failed-refresh logout now fires a one-time `notify` modal (`errors.session_expired` / `_body`, added to en/sq). Updated Network-state + Auth-flow sections and `STYLE_GUIDE → API Layer`. No backend/API change.
- **2026-06-17** — **Hybrid form error routing (`SKIP_GLOBAL_ERROR` → `INLINE_CLIENT_ERROR`).** Auth/change-password/register/reset mutations no longer suppress the global modal wholesale — they now route by error class: **client (4xx)** failures render inline on the form (field-actionable), while **unexpected** failures (5xx, network, timeout) fall through to the `apiError` modal (closing the silent-failure gap where a login 500 showed nothing). Renamed the meta const+key (`skipGlobalError` → `inlineClientError`); the global handlers gate on `meta.inlineClientError && isClientError(status)` (new helper, 4xx boundary). The inline side mirrors it: `authErrorMessage` (`features/auth/errors.ts`) now returns `string | undefined`, yielding `undefined` for 5xx/network so the form renders nothing and never doubles with the modal (login's JSX gates on the resolved message, not on `error`). 401/403/426 stay interceptor-owned (modal always skipped; 401 login still shows inline). Updated Network-state section + `STYLE_GUIDE → API Layer`. No backend/API change.
- **2026-06-17** — **Inline list error/empty states (data-screen polish).** Completes the global-error work: where the `apiError` modal is the loud one-time alert, the screen behind it now shows a quiet persistent state. Added reusable `components/empty/ListStateView` (shared title + subtitle + optional-Retry block) + `ErrorState` (generic `errors.list_failed_*` copy, always offers Retry → the query's `refetch`); refactored `EmptyChannelsState`/`EmptyEpgState`/`EmptyCatchupState` onto `ListStateView` (dedup, no deletion) and added `EmptyStationsState`. Home (TV + radio) and Guide now drive `ListEmptyComponent` off the full query state (skeleton → error → empty), reading the queries' `error`/`refetch`. New i18n: `errors.list_failed_title/_subtitle`, `empty.stations_title/_subtitle` (en/sq); `empty.channels_subtitle` reworded to empty-flavored (the error copy moved to `ErrorState`). Updated `STYLE_GUIDE → Loading States`. No backend/API change.
- **2026-06-22** — **"Now playing" matcher + server-driven Guide (`GET /api/v1/guide`).** Two related pieces, both keyed on programme start/end times. (1) **Channel screen play-icon** — the frozen mount-time `nowMs` was replaced by **`useNowProgram(programs)`** (`hooks/`), which derives the airing programme via the pure **`findPlayingProgram`** (`utils/epg.ts`), arms ONE `setTimeout` to the next programme edge (boundary-chained, **no poll, no network** — the day's EPG is already in memory), and re-evaluates on app foreground (RN throttles background timers). The "now" row + play-icon now rolls to the next programme at the boundary while the user sits on the channel watching live. Mirrors `useLiveParentalGuard`'s pattern. Only TODAY's list is passed (`[]` on other days → nothing marked). **No player/seek-bar change** — live stays full-bar-at-edge + non-seekable, catch-up stays seekable from 0 (current behavior was the desired one). (2) **Guide tab** — dropped the mock-EPG + frozen-`nowMs` derivation for a real **`GET /api/v1/guide?type=TV|RADIO`** call (`services/guide.ts` → `useGuideQuery(type)`, returns one `GuideChannel` per channel/station with the airing `now`). `type` mirrors the channels endpoint — the TV/Radio toggle drives one query switched by type (each value its own cache entry); TV rows open `channel/{id}`, radio rows open `radio/{id}`. This replaced the old "radio guide pending" empty-array stub. Progress bar comes from the pure **`programProgress(start, end, nowMs)`** (`utils/epg.ts`) using the query's **`dataUpdatedAt`** as a pure render clock (advances on each refetch; avoids an impure `Date.now()` in render). **Server-driven freshness, no polling** (decision 2026-06-22 — users pass through Guide quickly, a poll would add needless load): `staleTime: 60s` (overrides the global `Infinity` so triggers fire) + inherited `refetchOnWindowFocus`/`refetchOnReconnect` (app-foreground/reconnect) + **`useRefreshOnFocus(refetch)`** (new `hooks/` — canonical TanStack RN `useFocusEffect`, covers tab re-focus since Expo Router keeps tabs mounted) + pull-to-refresh. Endpoint returns `now` only → the GuideRow "Vijon…" (next) line is omitted until the backend adds `next`. Row tap → `channel/{channelId}` (id in each guide entry). Mock handler + `fixtures/guide.ts` added (kept for `mock` mode; runtime uses the real endpoint). `docs/API.md → Guide` section added. `decision`/age fields loose pending backend. **Known gaps:** no "next" line (endpoint lacks it); radio guide depends on the backend populating `now` for `type=RADIO` (the client is wired — rows render whatever the endpoint returns); Guide is a per-fetch snapshot — it won't roll over while you *stare* at the tab without leaving (intentional, no poll). **Wire shape (2026-06-22):** `GuideChannelDto` channel identity is `id` / `name` (not `channelId` / `channelName`), mapped to the domain `channelId` / `channelName` in `services/guide.ts`.
- **2026-06-22** — **Home channel list reuses the Guide refetch pattern.** `useChannelsQuery` (the list, not playback) gained `staleTime: 5min` + `gcTime: 15min` (overrides global `Infinity` / 1h) so foreground / reconnect / mount refetch triggers fire, and Home now calls `useRefreshOnFocus(refetch)` (same hook as Guide) for tab re-focus. Guide uses the same `5min` / `15min` pair. Channels + Guide are still **separate endpoints / query keys** (`['channels',type]` vs `['guide',type]`) — independent caches with independent freshness; this only aligns the *refetch triggers*, not the data. `useChannelPlaybackQuery` stays `staleTime: Infinity` (signed stream URLs). **Dead catch-up code removed (2026-06-22):** there is no `/catchup` endpoint, so `services/catchup.ts`, `useCatchupQuery`/`useCatchupItemQuery`, `CATCHUP_ROUTES`, the `/catchup` mock handlers + `fixtures/catchup.ts`, `CatchupCard`, the `CatchupItem` type, and the orphaned `program/[id]` route were deleted. In-channel catch-up is the EPG-by-date list + play-icon match, with `GET /channels/{id}/epg/{programId}` for recorded playback. `VodPlayer` was **kept** (its `program/[id]` host is gone) — it's the intended player for inline seekable recorded-programme playback; recorded programmes currently reuse `LivePlayer` (no seek bar yet).
- **2026-06-18** — **Stream layer replaced by `PlaybackDecisionDTO` from `GET /channels/{id}` (API reconciliation).** Deleted the invented `services/streams.ts` + `queries/useStreamQueries.ts` + `mocks/fixtures/streams.ts` — the `/streams/channel/{id}` endpoint does not exist. Stream URLs now come from the `PlaybackDecisionDTO` returned by `GET /channels/{id}`: `{ decision, channelId, programId, noticeMessage?, streams: Record<string, string> }` where `streams` carries `master` (ABR multivariant) plus optional fixed-rendition keys (`720`, `576`, `360`). EPG items embed the same playback fields so tapping a recorded programme swaps `activePlayback` state in `channel/[id].tsx` without an extra network request. `useChannelQuery` renamed `useChannelPlaybackQuery` (query key `['channel-playback', id]`); `useRadioStationQuery` rewritten as a find over the cached `useChannelsQuery('RADIO')` list (cache hit, no extra request). `resolveStreamSource` + `availableQualityIds` (`utils/resolveStreamSource.ts`) rewritten for `Record<string, string>`: maps `master` → `auto`, `720` → `720p`, etc. Channel metadata (name, `geoBlocked`) read from the `useChannelsQuery('TV')` list cache since `GET /channels/{id}` returns no metadata. Channel-level `isAdult` gate temporarily disabled (`needsPin = false`) — not present in list or `PlaybackDecisionDTO` yet. Live programme-level parental guard (`useLiveParentalGuard`) unaffected — reads `isAdult` from EPG items. Mock handlers updated to return `PlaybackDecisionDTO`; `MOCK_STREAMS` inlined; EPG fixtures embed playback fields. `docs/API.md → Channels` section added with full contract. `decision` stays `string` (loose) — exact union TBD with backend.
- **2026-06-22** — **Recorded playback seek bar via `LivePlayer isLive` prop (one player, not two).** Recorded/catch-up programmes now play through the **same** `LivePlayer` as live — the channel screen swaps `streamUrl` to the recorded URL and passes `isLive={selectedProgramId === null}`. The seek bar was already source-driven (`PlayerControls.isSeekable = duration > 0`): a recorded VOD reports a finite duration, so the bar becomes draggable from 0 automatically; the new `isLive` prop only drops the LIVE badge + the "pin to live edge" fallback. Live is unchanged (`isLive` true → full bar at edge, non-seekable). The player header shows the recorded programme's title (vs the channel name) while a recording plays. **Switching back to live** = tapping the now-airing `state === 'now'` row (today, play-icon) → `setSelectedProgramId(null)` + invalidate the live key (already wired in `channel/[id].tsx`; no pull-to-refresh on this screen). This supersedes the prior "recorded programmes reuse `LivePlayer` (no seek bar yet)" note and the plan to wire `VodPlayer` — **`VodPlayer` was deleted** (redundant; one player handles both). No backend/API change.
- **2026-06-23** — **Guide progress bar ticks in real time (client clock + boundary refetch).** The Guide's elapsed bar was driven by the query's `dataUpdatedAt` (the fetch timestamp), so it froze between refetches — the fill only advanced on pull-to-refresh / foreground / reconnect. Fixed by feeding a **ticking client clock** into `programProgress` instead: new **`useNow(intervalMs = 15s)`** hook (`hooks/`) holds `nowMs` in state, advances on a `setInterval`, and is **background-aware** (tears the interval down on background, re-arms + snaps forward on foreground, since RN throttles backgrounded timers). 15s default — a few-px bar moves <1px faster than that, so a tighter tick is wasted renders. Pattern mirrors RTSH's `useSound` interval (a *forced* poll there: `react-native-sound` emits no progress event) — but the Guide has no player, it derives `(now − start)/(end − start)` from EPG times already in memory, so a plain clock tick is the right (cheaper) tool. Separately, since the guide carries only `now` (not the full schedule), a programme ending while the user watches would peg its bar at 100% with no way to learn the next one — so the screen now arms a **single boundary `setTimeout`** to the earliest `now.end` and calls `refetch()` when it passes (chains to the next boundary as fresh data re-runs the effect; **still no poll**, consistent with the 2026-06-22 decision). Supersedes the 2026-06-22 "Guide is a per-fetch snapshot — it won't roll over while you stare" gap. `useNowProgram` / `useLiveParentalGuard` / `useGuideQuery` untouched. **Radio gap unchanged:** rows still show no bar when `/guide?type=RADIO` returns `now: null` — that's backend-side, not a client fix. No backend/API change.
- **2026-06-23** — **Player fullscreen is now orientation-driven + landscape captions un-clipped.** Two related player-UX fixes on `channel/[id].tsx`. (1) **Auto-fullscreen on rotation (YouTube/Netflix pattern).** Previously rotating the phone to landscape *without* tapping the fullscreen button left the screen in a broken half-state — the inline 16:9 player (`width:100%`) computed a height taller than the landscape viewport, overflowing it and zeroing the `ScrollView` height (couldn't scroll the EPG). Replaced the manual `useState(isFullscreen)` + the `lockAsync(LANDSCAPE)`-on-`isFullscreen` effect with the new **`useFullscreenOrientation()`** hook (in `hooks/useOrientation.ts`, built on the existing `useOrientation` shared `useSyncExternalStore`): **landscape ⇔ fullscreen**, derived purely (no state/effect — the react-hooks plugin bans setState-in-effect *and* ref-writes-in-render). The screen stays **sensor-unlocked** so physical rotation flows through; the fullscreen *button* still works in portrait via `enterFullscreen` (`lockAsync(LANDSCAPE)`), and `exitFullscreen` forces portrait then **re-arms the sensor after 1.5s** (`REARM_DELAY_MS`) so a later rotation re-enters. The broken inline-landscape state no longer exists (landscape is always fullscreen). **Stickiness** lives in the `useOrientation` store (`set` now ignores a late `unknown`, i.e. phone laid flat, once a real orientation is known) so fullscreen doesn't drop when the device goes flat — lint-clean because it's module scope, not render. `handleBack`: fullscreen → `exitFullscreen()`, else `router.back()` (back in fullscreen exits fullscreen, not the page — standard). (2) **Landscape caption clipping.** The custom fullscreen container was a raw inset-less `View`; expo-video draws native captions at the bottom of the `VideoView` bounds and exposes **no caption-padding API** (only iOS `contentPosition`), so captions/back-button landed under the home indicator / landscape notch. The fullscreen branch now renders through `ScreenLayout edges={['bottom','left','right']}` with the black `videoPlaceholderBg` token — `contain` re-centers the video into the safe area, captions clear the unsafe zones. **Known edge:** tapping exit while still physically sideways briefly forces portrait then (after re-arm) the sensor re-enters fullscreen — acceptable, users lower the phone to exit. No backend/API change.
- **2026-06-24** — **Unified parental gate: `useLiveParentalGuard` → `useParentalGuard`; recorded-path gate added.** Closed the "Catch-up/VOD program-level gate" known gap. The live-only guard was renamed to `useParentalGuard(channelId, { isLive, enabled })` and now owns both triggers behind one PIN / one `ParentalPinModal` / one per-id verified `Set`: (1) the existing **live** boundary-timed now-airing re-check (now gated on `isLive`, so a playing recording is no longer gated by the live schedule — a latent bug), and (2) a new **recorded** imperative `guardPlay(program, onAllowed)` that gates the tapped past programme on its own `EpgItem.isAdult` *before* the swap, so the signed stream URL is never fetched pre-PIN. `channel/[id].tsx` rewired: dropped the dead `needsPin = false` channel-level placeholder (channel-level gate intentionally stays off — no channel `isAdult`, and a per-programme gate avoids over-blocking a channel's clean recordings), `handleSelectProgram`'s `recorded` branch routes through `guard.guardPlay`, and the modal/overlay read `guard.{promptVisible,onVerified,onDismiss,blockedDismissed,requestUnlock}`. The gate always keys off the EPG row (the `PlaybackDecision` carries no `isAdult`). `requestUnlock`/`onVerified`/`onDismiss` are plain functions (not `useCallback`) — React Compiler memoizes them and they're JSX-prop-only; `useLiveParentalGuard.ts` deleted, barrel + `useNowProgram` JSDoc updated. tsc + lint clean. No backend/API change.
