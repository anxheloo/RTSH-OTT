/**
 * Brand components barrel — RTSH logo mark and the surfaces that use it.
 * Import from '@/components/Brand' instead of individual files.
 */
export { default as BrandHeader } from './BrandHeader';
