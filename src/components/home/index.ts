/**
 * Home components barrel — composite cards/rails for the Home screen.
 * Import from '@/components/home' instead of individual files.
 */
export { default as HeroCarousel } from './HeroCarousel';
